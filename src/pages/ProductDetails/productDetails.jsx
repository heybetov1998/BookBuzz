import styles from "./productDetails.module.scss";
// import { useParams } from "react-router-dom";

const ProductDetails = () => {
    // const { id } = useParams();

    return <section className={styles.productDetailsPage}></section>;
};

export default ProductDetails;
